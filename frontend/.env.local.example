# Mapbox access token for the campus map (get one at https://account.mapbox.com/)
NEXT_PUBLIC_MAPBOX_TOKEN=your_mapbox_token_here

# Backend API base URL for density heatmap data (default: http://localhost:5000)
NEXT_PUBLIC_API_URL=http://localhost:5000
